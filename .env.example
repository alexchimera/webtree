# PostgreSQL connection string.
# Local dev (matches docker-compose.yml):
DATABASE_URL="postgresql://webtree:webtree@localhost:5432/webtree?schema=public"

# On Railway: add a PostgreSQL plugin and reference its connection string, e.g.
# DATABASE_URL="${{Postgres.DATABASE_URL}}"
